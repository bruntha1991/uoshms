{% extends '::base.html.twig' %}

{% block container -%}
<div class="span6">
    <div class="containerHome" >
    <h1>Room list</h1>
<script src="assets/js/jquery.tablesorter.min.js"></script>
        <script >
            $(function() {
                $("table#sortTableExample").tablesorter({sortList: [[1, 0]]});
            });
        </script>
        <table class="table">
        <thead>
            <tr>
                <th>Room Number</th>
                <th>Type</th>
                <th>Monthly cost</th>
                
                <th>Hall Name</th>
                
            </tr>
        </thead>
        <tbody>
            
        {% for entity in entities %}
            {% if entity.hall== hallname %}
            <tr>
                <td><a href="{{ path('room_show', { 'id': entity.id }) }}">{{ entity.roomno }}</a></td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.monthlycost }}</td>
                
                <td>{{entity.hall}}</td>
                <td>
                <ul>
                    <li class="nav-header">
                        <a href="{{ path('room_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li class="nav-header">
                        <a href="{{ path('room_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
            {% endif %}
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li class="nav-header">
            <a href="{{ path('room_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
{%block sideMenu %}

<ul class="nav nav-list">
                <li  class="nav-header" ><b>Home</b></li>
              
              <li class="nav-header">UOS Halls</li>
              <li ><a  href="{{ path('hall')}}">View All </a></li>
              <li ><a href="{{ path('hall_new')}}">Add Hall</a></li>
              
              <li class="nav-header">UOS Rooms</li>
              <li ><a href="{{ path('room')}}">View All</a></li>
              <li><a href="{{ path('room_new')}}">Add Room</a></li>
              
              <li class="nav-header">UOS Student Management </li>
              <li class="active"><a href="{{ path('student')}}">View All Students</a></li>
              <li><a href="{{ path('student_new')}}">Add Student</a></li>
              
              <li class="nav-header">UOS Student Accomodation </li>
              <li><a href="{{ path('student')}}">Accomodation</a></li>
              <li><a href="{{ path('student')}}">Add Accomodation</a></li>
              
              <li class="nav-header">UOS Employee Management </li>
              <li><a href="{{ path('employee')}}">Employee</a></li>
              <li><a href="{{ path('employee_new')}}">Add Employee</a></li>
              
              <li class="nav-header">UOS Employee Positining </li>
              <li><a href="{{ path('student')}}">Positions</a></li>
              <li><a href="{{ path('student')}}">Add Position</a></li>
              
              <li class="nav-header">UOS Accounts </li>
              <li><a href="{{ path('student')}}">Account Details</a></li>
              <li><a href="{{ path('student')}}">Add Account</a></li>
              
            </ul>
{%endblock%}